This project was created for practice using Hooks like, useContext, useLocation, useNavigation, customHooks and React Router.

## Available Scripts

In the project directory, you can run:

### `npm start`
